rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // COMPLETELY OPEN - Public API style
    // Anyone can read, write, update, delete anything
    // Perfect for public recipe API
    match /{document=**} {
      allow read, write, update, delete: if true;
    }
  }
}