rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Extremely permissive rules - allow all reads and writes to all documents
    match /{document=**} {
      allow read, write: if true;
    }
  }
}